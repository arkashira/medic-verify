[project]
name = "medic-verify"
version = "0.1.0"
description = "Simple AI model validation for healthcare data with compliance checks."
authors = [{name = "Axentx"}]
requires-python = ">=3.8"
dependencies = []
